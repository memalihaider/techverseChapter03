
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
  domains: ['images.pexels.com', 'randomuser.me', 'drive.google.com'],
  },
}

module.exports = nextConfig;
